#include <iostream>
#include <string>
using namespace std;

string shorten(string u){
  /*
   * Encode the url with [A-Za-z0-9.?-+!/:]
   */

}

int main(){
  string url;
  cin>>url;
  string shorten_url = shorten(url);
  cout<<"The shorten URL = "<<shorten_url<<endl;

  return 0;
}
